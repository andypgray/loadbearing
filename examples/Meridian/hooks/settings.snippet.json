{
  "permissions": {
    "deny": ["Bash(loadbearing baseline*)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -File \"${CLAUDE_PROJECT_DIR}/.claude/arch-hook.ps1\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
