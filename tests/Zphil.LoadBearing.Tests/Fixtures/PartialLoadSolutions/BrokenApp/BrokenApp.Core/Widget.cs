namespace BrokenApp.Core
{
    // Ordinary source in the half that loads. Its presence is what makes the survey look plausible: a
    // partial model is not empty, it is quietly short of a project.
    public class Widget
    {
        public string Name { get; set; }
    }
}
