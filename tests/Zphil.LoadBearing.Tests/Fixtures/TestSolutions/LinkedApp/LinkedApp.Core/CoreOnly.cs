namespace LinkedApp.Core;

// Declared by this project alone. LinkedApp.Client references it through a real ProjectReference, which
// is what makes the genuine Client -> Core edge distinguishable from the linked-source phantom.
public class CoreOnly
{
}
